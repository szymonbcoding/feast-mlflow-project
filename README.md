# feast-mlflow-project

Project contains simple workflow of creating features and tracking ml model training. Flow is saved in Jupyter Notebook - _project.ipynb_.

Future plans:
* Run MLflow Tracking Server
* Deploy ML model